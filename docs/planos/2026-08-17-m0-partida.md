# M0 — Partida (semanas de 17 e 24/08/2026)

Objetivo: sair de "spec pronta" para "equipe de três produzindo com trilho".
**Não é fase de funcionalidade.** Nada aqui aparece para o usuário final — e é justamente por isso
que costuma ser pulada, e por isso os outros projetos da casa nasceram sem ela.

Critério de saída: **Hugo e Matheus conseguem clonar, rodar e abrir um PR que passa no CI, sem
perguntar nada a ninguém.**

---

## 1. Repositório e convenções — antes de qualquer `npm install`

- [ ] `git init`, repositório remoto, branch `main` protegida
- [ ] `CLAUDE.md` (feito) — ler antes de qualquer tarefa
- [ ] `docs/decisoes/0001-decisoes-fundacionais.md` (feito)
- [ ] `docs/planos/` e `docs/evolucao/` criados com este arquivo como primeiro exemplo
- [ ] `.editorconfig` + `.gitignore` + `.nvmrc`
- [ ] `README.md` curto: o que é, como rodar, onde estão as decisões

## 2. Projeto Next + qualidade desde o primeiro commit

- [ ] `npm create next-app` — TypeScript, App Router, Tailwind 4
- [ ] **ESLint configurado e rodando** (`npm run lint` precisa funcionar — no SisEscala nunca funcionou)
- [ ] Prettier ou formatação padronizada, acordada entre os três
- [ ] **Vitest** instalado com um teste real em `src/core/` (nem que seja o cálculo de consumo)
- [ ] Scripts: `dev`, `build`, `lint`, `test`, `typecheck`

## 3. CI — o trilho que substitui a disciplina

- [ ] GitHub Actions em PR e push na `main`: `lint` → `typecheck` → `test` → `build`
- [ ] PR não mergeia com CI vermelho
- [ ] (depois, quando houver migrations) `supabase test db` com pgTAP no mesmo workflow

## 4. Supabase

- [ ] Projeto novo, exclusivo do SISFROTA (D1)
- [ ] Supabase CLI ligado ao projeto, `supabase/migrations/` versionado
- [ ] Primeira migration: extensões, `frota_veiculos`, `frota_medidor_leituras` (append-only, D4)
- [ ] **RLS habilitada com DENY padrão já na primeira migration** — nunca "depois"
- [ ] Bucket privado de evidências + política de acesso
- [ ] Backup automático confirmado

## 5. Coolify

- [ ] App criado, Dockerfile, deploy por push
- [ ] Subdomínio `sisfrota.maraba.pa.gov.br` + HTTPS
- [ ] Variáveis de ambiente separadas de desenvolvimento
- [ ] Deploy de uma página "hello" para provar o caminho ponta a ponta

## 6. Ferramenta do censo — a única coisa de M0 que vira valor visível

Precisa estar pronta antes de a dupla de campo entrar em operação (set/26):

- [ ] Importador CSV com validação de placa, RENAVAM e chassi, e relatório de divergências
- [ ] Tela de conciliação (planilha × patrimônio × contratos)
- [ ] Formulário mobile de inventário: foto do veículo, foto do painel, placa, chassi, estado, GPS
- [ ] Geração das etiquetas QR para colar nos veículos
- [ ] Exportação do **relatório de inventário** em PDF

## 7. Divisão do time (três frentes com pouca sobreposição)

| frente | conteúdo |
|---|---|
| **Banco e integrações** | migrations, RLS, espelho, API de Diretório no SisEscala |
| **Painel administrativo** | cadastros, censo, conciliação, relatórios |
| **PWA do motorista** | offline, câmera, fila de sincronização, modo assistido |

Matheus está entrando: primeira tarefa dele deve ser **na frente do painel**, que é a de menor
risco e a que melhor ensina as convenções do projeto. PWA offline é a mais difícil das três —
não é tarefa de quem está chegando.

---

## O que **não** fazer em M0

- Não modelar as 40 tabelas de uma vez. Só o que o censo usa.
- Não começar o PWA. Ele depende de cadastro que ainda não existe.
- Não prometer data de funcionalidade antes de o censo dizer o tamanho real da frota.
